# Лабораторная работа №1

Приведён отчёт по лабораторной работе №1 "Основы языка ассемблера"

--------
## Задание 1 "Арифметика и переменные"

**Файл:** `1.asm`

В секции `.bss` был выделен qword под результат вычислений. До вычислений:

![Result до вычислений](/img/1_1.png)

После вычислений:

![Result после вычислений (hex)](/img/1_2.png)

![Result после вычислений (dec)](/img/1_3.png)

Для выполнения операций использовались регистры rax (хранение промежуточного результата) и rdx (хранение промежуточных операндов). Сначала первые два операнда были скопированы в регистры:

![Регистры rax и rdx в начале вычислений](/img/1_4.png)

Затем была выполнена инструкция `add rax, rdx`:

![Промежуточные значения регистров](/img/1_5.png)

Затем в rdx была скопирована переменная c:

![Значение rdx после копирования c](/img/1_6.png)

Далее последняя операция `sub rax, rdx`, после чего в rax оказался итоговый ответ:

![Итоговое значение rax](/img/1_7.png)

В случае сборки с отладочными символами (`-g -F dwarf`) в отладчике отображается исходный код программы и его строчка, которая в данный момент выполняется:

![Исходный код в отладчике](/img/1_8.png)

## Задание 2 "Массивы и циклы"

**Файл:** `2.asm`

1) Содержимое массива в десятичном и в шестнадцатеричном виде:

![Содержимое массива (dec и hex)](/img/2_1.png)

2) Рассмотрим инструкцию `cmp rcx, 5`, где `rcx=1`. EFLAGS до инструкции:

![EFLAGS до инструкции](/img/2_2.png)

Активен только флаг IF. После инструкции дело поменялось:

![EFLAGS после инструкции](/img/2_3.png)

В итоге:

* CF=1, в результате 1-5 произошёл заём
* PF=1, младший байт (1-5=fc=252) чётен
* AF=1, произошла коррекция
* ZF=0, результат ненулевой
* SF=1, результат операции отрицателен
* TF=0, не устанавливался
* IF=1, прерывания разрешены
* DF=0, не устанавливался 
* OF=0, переполнение не произошло

3) Регистр RIP указывает на операцию, которая только будет выполнена. До выполнения movabs RIP указывал на неё:

![RIP до выполнения movabs](/img/2_4.png)

После неё:

![RIP после выполнения movabs](/img/2_5.png)

4) В RAX я помещал число из массива, с которым будет сравниваться r8. В конце цикла он равен 42 (последнему числу из массива):

![RAX в конце цикла](/img/2_6.png)

Ну вот и

![ВСЁ!](/img/3.png)

